[package]
name = "ashfall-materials"
version.workspace = true
edition.workspace = true

[lib]
name = "ashfall_materials"
path = "src/lib.rs"

[dependencies]
ashfall-core = { path = "../ashfall-core" }
