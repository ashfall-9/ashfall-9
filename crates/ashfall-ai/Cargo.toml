[package]
name = "ashfall-ai"
version.workspace = true
edition.workspace = true

[lib]
name = "ashfall_ai"
path = "src/lib.rs"

[dependencies]
ashfall-core = { path = "../ashfall-core" }
candle-core = "0.10.2"
